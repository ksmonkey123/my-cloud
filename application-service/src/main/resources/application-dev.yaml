spring:
  jpa:
    show-sql: true

logging:
  level:
    org.springframework.jdbc.core: DEBUG

canary:
  sender: "test@awae.ch"
  timer:
    web: "*/30 * * * * *"
    dockerhub: "0 * * * * *"

shedlock:
  default:
    min-lock: PT10S