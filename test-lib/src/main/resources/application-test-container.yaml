spring:
  datasource:
    url: jdbc:tc:postgresql:18.1:///testdb
    username: user
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

logging:
  level:
    org.springframework.jdbc.core: DEBUG
    ch.awae.mycloud: DEBUG